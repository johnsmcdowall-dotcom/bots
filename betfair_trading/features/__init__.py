"""Shared market-microstructure feature computation (WOM, order-book
imbalance, microprice, price/volume velocity & acceleration, rolling
windows).

Empty scaffold as of Phase 1. Built out from Phase 3 per docs/PLAN.md.
Every feature added here must be shown (permutation importance/ablation)
to add genuine out-of-sample value before it feeds a model — see
docs/ARCHITECTURE.md.
"""
